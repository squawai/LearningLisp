;; the function Pyramid
;; calculates a colume of a pyramid whose bottom (base?) is a square with area 24
;; when the height of is input form keyboard



;(define (Pyramid h)
;  (* h 8.0))

;(define (volume ( * (read) 8 )))

(display "Enter Height: ")
;(let h (read))
(display "Volume: ")
(write ( * (read) 8.0))
;(write volume))
;(Pyramid)
